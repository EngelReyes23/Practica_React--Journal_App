const initialState = {
  notes: [],
  activeNote: {},
};

export const notesReducer = (state = initialState, action) => {
  switch (action.type) {
    // case value:

    //   break;

    default:
      return state;
  }
};
